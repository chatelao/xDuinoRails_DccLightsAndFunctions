#ifndef FASTLED_SAFE_H
#define FASTLED_SAFE_H

// We are suppressing ArduinoSTL's new operator via compat/ArduinoSTL_AVR_Compat.h
// So we rely on FastLED to provide placement new.
#include <FastLED.h>

#endif // FASTLED_SAFE_H
